'use client'; import React from 'react'; import type { Region } from '../lib/types'; export default function Filters({regions,value,onChange}:{regions:Region[];value:any;onChange:(v:any)=>void}){ return (<form onSubmit={(e)=>e.preventDefault()} style={{display:'grid',gridTemplateColumns:'1fr 1fr 1fr 1fr',gap:8}}><input placeholder='q' value={value.q||''} onChange={e=>onChange({...value,q:e.target.value})}/><select value={value.kind||''} onChange={e=>onChange({...value,kind:e.target.value||undefined})}><option value=''>Type</option><option value='indus'>Indus</option><option value='friche'>Friche</option><option value='patrimonial'>Patrimonial</option></select><select value={value.minScore||''} onChange={e=>onChange({...value,minScore:e.target.value?Number(e.target.value):undefined})}><option value=''>Score</option><option value='2'>≥2</option><option value='3'>≥3</option><option value='4'>≥4</option></select><select value={value.region||''} onChange={e=>onChange({...value,region:e.target.value||undefined})}><option value=''>Région</option>{regions.map(r=><option key={r.code} value={r.code}>{r.name}</option>)}</select></form>); }
