{
  "name": "agregateur-fr",
  "version": "1.0.0",
  "private": true,
  "pnpm": {
    "overrides": {
      "@fastify/helmet": "12.0.1"
    }
  },
  "scripts": {
    "seed:ingest": "tsx tools/ingest-pdf.ts",
    "geo:build": "tsx tools/build-geojson.ts",
    "seed:all": "pnpm seed:ingest && pnpm geo:build"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "pdf-parse": "^1.1.1",
    "@types/pdf-parse": "^1.1.4"
  }
}
